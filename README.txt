Решение задач Семинар№2
Команды:
dotnet new console
dotnet run
git add .
git commit -m "TaskHome38"
git commit -m "Семинар№8 Домашнее задание Решена задача 56"
git push
git status
clear
Console.WriteLine();

Console.ReadLine();



/*дддд*/ - многострочный комментарий, то есть закомитили все строки

Тип данных например int   int x = 2 (выдели для переменной х = 2 такой объем памяти)

Console.WriteLine($"Сумма = {sum}, разница = {dif}");
Console.WriteLine($"Корни уравнения x1 = {x1},  x2 = {x2}");

При расчете сначала действия слева направо и в скобках приоритет(в первую очередь скобки)

Пример как узнать размер Типа данных : int i = int.MaxValue;

Конвертация вводной строки в число:  int a = int.Parse(Console.ReadLine());
Строку можно сделать числом с помощью Конвертации.Сначала все строка!

Массив  int[] array = {1,3,5,4,7} или int[] array = {5} или int[] array = new array {5}
Изначально массив заполненен "0"

Случайный набор чисел Random().New()

sting i = null или sting i = ""; - строка существует, но навыходе будет пустота 
то есть 0, из за этого много ошибок.Значит: Создай строку но не клади в нее ничего!

Тип данных "bool" это истина или ложь - сравнение для чисел! Строки сравнивать не можем!
"==" на равенство строки можем сравнивать, но больше иил меньше не можем!

switch (i) - вместо if else
{
    case 0:
    Console.WriteLine("Ноль");
    break;
    case 1:
    Console.WriteLine("Единица");
    break;
}

Цикл do while один раз обязательно выполнится при любом условии!
ПРИМЕР1:
int k = 13;
do
{
   Console.WriteLine(k); 
} while (k<10);
ОТВЕТ : 13 то есть 1 раз цыкл выполнился, зотя по усл к<10

ПРИМЕР2:
int k = 13;
do
{
   Console.WriteLine(k); 
   k++;
} while (k<17);
ОТВЕТ : 13,14,15,16

Цикл  foreach 
ПРИМЕР:
string[] days = new string[7] {"Понед", "Вт", "Ср", "Чет", "Пят", "Субб", "Воскр"}; 
foreach (string day in days)
{
    Console.WriteLine(day); 
}

Пример табуляции :
Console.WriteLine($"\t{i+1}"); 
Ответ будет выводится с отступами

Метод или Функция :
void Variant1 ()
{
    
}








